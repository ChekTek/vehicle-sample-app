/** There's definitely a better way to organize this data,
 *  but it's just a drill to demonstrate angular data flow **/
export interface PartDetails {
  partId: number;
  subCategory: string;
  description: string;
  cost: number;
}
